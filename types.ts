export interface FormState {
  brand_text: string;
  product_quantity: number;
  brand_text_color: string;
  brand_text_layout: string;
  theme: string;
  aspect_ratio: string;
  reference_images: string[];
}